# Foo

The thing works. ([validated by](tests/linked.test.ts#L2))

A code link is not a test link. ([implemented by](src/foo.ts#L1))

A dir-relative href resolves to the repo-root key. ([validated by](../../tests/tagged.test.ts#L1))
